<%= bootstrap_form_with(model: pet, local: true, control_col: "col-sm-4") do |form| %>
  <% if pet.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(pet.errors.count, "error") %> prohibited this pet from being saved:</h2>

      <ul>
      <% pet.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <%= form.text_field :name, label: 'Nome' %>
  <%= form.date_field :date_of_birth, label: 'Data de Nascimento' %>
  <%= form.text_field :breed, label: 'Raça' %>
  <%= form.select :type_of_animal, Pet.type_of_animals.keys, label: 'Tipo de animal'%>
  <%= form.form_group do %>
    <%= form.submit "Submeter", class: 'btn btn-success' %>
  <% end %>
<% end %>
